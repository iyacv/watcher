.env
__pycache__/
*.pyc
inbox/
processed/
failed/

# Build outputs — never commit these
dist/
build/downloads/
*.db
*.db-journal

# Runtime log (rotating) — generated at run time, not source
watcher.log
watcher.log.*
